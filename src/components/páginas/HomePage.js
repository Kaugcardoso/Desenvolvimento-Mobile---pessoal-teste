import React from 'react';
import { View, Text } from 'react-native';
import styles from '../../styles/HomePageStyles';

const HomePage = () => {
    return (
        <View style={styles.container}>
            <Text>Bem Vindo ao Estoque JJK!</Text>
        </View>
    );
};

export default HomePage;
