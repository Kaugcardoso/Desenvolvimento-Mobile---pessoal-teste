import React from 'react';
import { View, Text } from 'react-native';
import pageStyles from '../../styles/PageStyles';

const Fornecedor = () => {
    return (
        <View style={pageStyles.container}>
            <Text style={pageStyles.title}>Fornecedor</Text>
        </View>
    );
};

export default Fornecedor;
