import React from 'react';
import { View, Text } from 'react-native';
import pageStyles from '../../styles/PageStyles';

const Produto = () => {
    return (
        <View style={pageStyles.container}>
            <Text style={pageStyles.title}>Produto</Text>
        </View>
    );
};

export default Produto;
